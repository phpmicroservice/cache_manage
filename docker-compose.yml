version: '2'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: cache_manage:dev
    privileged: false
    restart: unless-stopped
    tty: true
    volumes:
    - .:/var/www/html
  
